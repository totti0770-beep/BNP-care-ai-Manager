# Kept in step with the root pyproject.toml, which is what Replit installs.
# langchain-community and pypdf were missing here, so the documented
# `pip install -r requirements.txt && uvicorn main:app` failed with
# ModuleNotFoundError anywhere other than Replit.
#
# Lower bounds only; use uv.lock (or pip-compile) for a reproducible install.
fastapi>=0.135.3
uvicorn[standard]>=0.43.0
python-multipart>=0.0.22
PyJWT>=2.12.1
psycopg2-binary>=2.9.11
alembic>=1.13.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.2.2

# Retrieval
faiss-cpu>=1.13.2
numpy>=2.4.4
rank-bm25>=0.2.2

# Document ingest and generation
langchain>=1.2.15
langchain-community>=0.4.1
langchain-openai>=1.1.12
langchain-text-splitters>=1.1.1
openai>=2.30.0
pypdf>=6.9.2
tiktoken>=0.12.0

# Formulary import and the pharmacist review packet
openpyxl>=3.1.5
